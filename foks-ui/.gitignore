# Screenshots the Playwright acceptance run writes. They are an artefact of a
# run, not a checked-in baseline: the gate is zero console errors and
# `scrollWidth === 1280`, and the by-eye comparison is done against
# `dev/foks-desktop/iteration/wave6/`, which is in the repository already.
tests/acceptance/shots/
